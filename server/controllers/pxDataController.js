import dbConnection from '../db.js';

async function pxData (req, res){
    try{
            const userID = 
            const pxData = await dbConnection.query('SELECT user_id FROM test_px_table WHERE user_id = ', [email])
    }
}
export { pxData };